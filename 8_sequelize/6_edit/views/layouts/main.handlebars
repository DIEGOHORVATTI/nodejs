<!DOCTYPE html>

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <link rel="icon" type="image/x-icon" href="./mikey.co">
  <link rel="stylesheet" href="/css/styles.css">
  <link rel="stylesheet" type="text/css" href="/css/lofy.css"/>
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.4.1/jquery.min.js"></script>
</head>

<body>
  <div class="fundo"></div>
  <nav id="navbar">
    <div>
      <ul>
        <li><a href="/">home</a></li>
        <li><a href="/users/create">Cadastrar links</a></li>
      </ul>
    </div>
  </nav>
  {{{ body }}}

  <div id="player"></div>

  <div class="loft_menu">
    <div class="loft_menu_header">
      <div class="relativo">
        <img class="img_menu" src="https://yt3.ggpht.com/ytc/AKedOLTkvv3TyNLJYEWXsPdCsgg8fvSn9-Oj65GBhkAiEg=s68-c-k-c0x00ffffff-no-rj" height="50px">
        <div class="text_menu">Lofy&nbspGir<span class="Live">LIVE</span></div>
          <div class="text_menu_body">
              <div class="form-check">
                <label class="form-check-label form-check-toggle">
                    <input id="input_1" class="form-check-input" type="checkbox" value="true"/><span></span>
                </label>
              </div>
          </div>
      </div>
    </div>
  </div>

  <script>
  $('#input_1').change(function () {
    if (this.checked) {
      playVideo()
    } else {
      stopVideo()
    }
  })
    
  var tag = document.createElement('script');

  tag.src = "https://www.youtube.com/iframe_api";
  var firstScriptTag = document.getElementsByTagName('script')[0];
  firstScriptTag.parentNode.insertBefore(tag, firstScriptTag);

  var player;
  function onYouTubeIframeAPIReady() {
    player = new YT.Player('player', {
      height: '0',
      width: '0',
      videoId: '5qap5aO4i9A',
      events: {
        'onReady': onPlayerReady,
        'onStateChange': onPlayerStateChange
      }
    });
  }

  function onPlayerReady(event) {
    setTimeout(stopVideo, 60000000);
    event.target.playVideo();
  }

  var done = false;
  function onPlayerStateChange(event) {
    if (event.data == YT.PlayerState.PLAYING && !done) {
      done = true;
    }
  }
  function stopVideo() {
    player.stopVideo();
  }
  function playVideo() {
    player.playVideo();
  }
  </script>

</body>
</html>